#include <stdio.h>

// Declaración de la función a implementar
int suma(int a, int b);
    return a + b;


int main() {

    // Ejemplo de uso rápido
    printf("Resultado: %d\n", suma(2, 3));
    return 0;
}
